# Server
LISTEN_ADDR=0.0.0.0
LISTEN_PORT=8080
READ_TIMEOUT=5s
WRITE_TIMEOUT=10s
IDLE_TIMEOUT=30s

# Download Test
DOWNLOAD_BUFFER_SIZE=50          # Size of pre‑generated random buffer (MB)
DOWNLOAD_MAX_SIZE=500         # Max allowed download size per request, 0 for none

# Upload Test
UPLOAD_MAX_SIZE=100

# Logging
LOG_LEVEL=info                        # debug, info, warn, error
LOG_FILE=""                           # path to write log data```
